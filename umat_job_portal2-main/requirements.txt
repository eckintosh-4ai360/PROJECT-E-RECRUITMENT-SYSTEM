############################################
# UMat Job Portal - Cleaned Requirements   #
# Notes (Windows):                         #
# - Install PyTorch and sentence-transformers MANUALLY (see below) to avoid wheel issues.
# - Download spaCy models MANUALLY after installing spaCy.                
############################################

# --- Core web stack ---
Flask==2.0.1
Werkzeug==2.0.1
Flask-SQLAlchemy==2.5.1
SQLAlchemy==1.4.23
Flask-Login==0.5.0
Flask-WTF==0.15.1
WTForms==2.3.3
Flask-Mail==0.9.1
Flask-Bootstrap==3.3.7.1
email-validator==1.1.3
python-dotenv==0.19.0
PyMySQL==1.0.2
markupsafe==2.0.1

# --- Database migrations (used by scripts in migrations/) ---
alembic>=1.11

# --- File handling / resume parsing ---
pdfminer.six>=20220524
python-docx>=0.8.11
Pillow>=9.0.0
python-dateutil>=2.8.2

# --- NLP / Data stack ---
spacy==3.8.7
nltk>=3.8.1
pandas>=1.5.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0
flashtext>=2.7

# --- Advanced NLP (Transformers) ---
# MANUAL: torch (choose correct wheel for your OS/Python/CUDA) → https://pytorch.org/get-started/locally/
# MANUAL: sentence-transformers (depends on torch; install AFTER torch)
# Suggested commands (CPU example):
#   pip install --upgrade pip
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision torchaudio
#   pip install "sentence-transformers>=2.2.2"

# --- spaCy models (not pip packages) ---
# After installing spaCy, run:
#   python -m spacy download en_core_web_md

# --- Optional/Not currently used by the codebase ---
# requests                 # Not imported anywhere currently
# plotly                   # Optional visualization only
# gunicorn                 # Linux/Heroku deployment only; not needed on Windows
# twilio>=8.2.0            # SMS integration; no current imports
# gensim>=4.1.0            # Not used
# spacy-transformers>=1.1.2# Not used directly (spaCy itself is used)
# PyPDF2==3.0.1            # Not used (using pdfminer.six instead)
# transformers>=4.41.0     # Pulled transitively by sentence-transformers if needed
